import { CircularProgress } from '@mui/material'
import React, { FC } from 'react'

const Loader: FC = () => {
    return (
        <div>
            <CircularProgress />
        </div>
    )
}

export default Loader